name: NanoServer Test

on: [push, workflow_dispatch]

jobs:
  run-nanoserver:
    runs-on: windows-latest
    steps:
      - name: Pull Nano Server Image
        run: |
          docker pull mcr.microsoft.com/windows/nanoserver:ltsc2025

      - name: Run a Command in Nano Server
        run: |
          docker run --rm mcr.microsoft.com/windows/nanoserver:ltsc2025 cmd /c echo Hello from Nano Server
